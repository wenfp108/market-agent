name: ⚔️ PolyData Dual-Engine System

on:
  schedule:
    - cron: '15 * * * *'  # 保持每小时 15 分自动触发
  workflow_dispatch: 

jobs:
  engage_targets:
    runs-on: ubuntu-latest
    
    env:
      MY_PAT: ${{ secrets.MY_PAT }} # 使用你的 PAT 确保有权限推送
      REPO_OWNER: ${{ github.repository_owner }}
      GITHUB_REPOSITORY: ${{ github.repository }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libnss3 libatk-bridge2.0-0 libcups2 libgbm1 libasound2t64 libpangocairo-1.0-0 libgtk-3-0

      - name: Install Project Dependencies
        run: npm ci

      - name: 🎯 Run Sniper Engine
        run: node sniper.js

      - name: 📡 Run Radar Engine
        run: node radar.js

      # 🌟 新增：将猎物带回家（提交捕获的数据）
      - name: 📤 Push Captured Data
        run: |
          git config --global user.name 'Architect Bot'
          git config --global user.email 'bot@architect.alpha'
          # 只添加 data 目录下的新 JSON 文件
          git add data/
          # 如果没有新数据产生，commit 会返回 0 而不会导致工作流失败
          git commit -m "Capture: New signals secured $(date +%F)" || exit 0
          git push
